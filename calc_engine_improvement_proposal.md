# Calc Engine Improvement Proposal

본 문서는 `python_backend/calc_engine.py`의 코드 분석을 통해 발견된 ad-hoc 로직의 개선 사항과 코드 품질 향상을 위한 제안을 담고 있습니다.

## 1. 개선된 Ad-hoc 로직 (수정 완료)

기존 코드에서 특정 상황에만 동작하도록 하드코딩된 로직들을 더 일반적인 상황에서도 사용할 수 있도록 다음과 같이 개선하였습니다.

### 1.1. 텐서 확장 (`op_tensor_expand`)
*   **기존**: 차원이 항상 1~3으로 고정되어 있었습니다.
*   **개선**: `parallelOptions`를 통해 `dim=N` 인자를 받아 원하는 차원까지 확장할 수 있도록 변경하였습니다. (기본값은 3 유지)
*   **사용 예시**: `calc > tensor_expand / dim=4`

### 1.2. 테일러 급수 전개 (`op_taylor`)
*   **기존**: 전개 지점이 항상 0(Maclaurin series)으로 고정되어 있었으며, 차원 결정 로직이 제한적이었습니다.
*   **개선**: `at=N` 또는 명령의 세 번째 인자를 통해 전개 지점을 자유롭게 설정할 수 있도록 하였으며, `order=N` 명시적 인자를 지원합니다.
*   **사용 예시**: `taylor > x, 4, 1` (x=1 지점에서 4차까지 전개)

### 1.3. 수치적 ODE 풀이 (`op_num_solve`)
*   **기존**: 시간 범위(`t_span`)가 [0, 10]으로 고정되고, 샘플링 포인트가 100개로 하드코딩되어 있었습니다.
*   **개선**: `t_span=start,end` 및 `points=N` 인자를 지원하여 더 정밀하거나 넓은 범위의 계산이 가능하도록 하였습니다. 또한 결과 출력 시 샘플 인덱스를 전체 포인트 수에 비례하여 유동적으로 선택하도록 개선하였습니다.

## 2. 코드 정리 및 불필요한 요소 제거 (수정 완료)

### 2.1. 주석 정리
*   코드 곳곳에 산재해 있던 `[cite: ...]` 형태의 불필요한 메타데이터 주석을 모두 제거하여 가독성을 높였습니다.

### 2.2. 명령어 라우터 수정
*   `tensor_expand` 등 일부 명령에서 `parallelOptions`가 누락되어 확장이 불가능했던 문제를 해결하였습니다.

## 3. 추가 개선 제안 (피드백 요청)

현재 구조에서 더 나은 품질을 위해 다음과 같은 사항들을 제안합니다.

### 3.1. 유틸리티 코드의 중앙 집중화
*   현재 `SAFE_SYMPY_DICT`, `strip_latex_delimiters` 등이 `calc_engine.py`, `matrix.py`, `dimcheck_engine.py` 등에 중복 정의되어 있습니다.
*   **제안**: `python_backend/utils.py`를 생성하여 공통 함수와 상수를 관리함으로써 코드 중복을 줄이고 유지보수성을 높여야 합니다.

### 3.2. 독립 변수 감지 로직의 정교화
*   `fix_ode_expression` 등에서 `g`, `L`, `k` 등을 상수로 간주하여 제외하고 있으나, 이는 물리 문제의 맥락에 따라 달라질 수 있습니다.
*   **제안**: 사용자가 `const=g,L` 처럼 상수를 명시적으로 지정할 수 있는 옵션을 추가하거나, 수식 내에서 미분 대상이 아닌 변수들을 더 지능적으로 분류하는 로직이 필요합니다.

### 3.3. 에러 핸들링의 세분화
*   현재 `execute_calc`는 거대한 `try-except` 블록으로 감싸져 있어 에러의 원인을 정확히 파악하기 어려울 때가 있습니다.
*   **제안**: 각 연산 단계별로 커스텀 예외(Exception)를 정의하고, 사용자에게 더 친절한 수학적 오류 메시지를 제공해야 합니다.

### 3.4. 단위 테스트 확충
*   현재 Python 백엔드 로직을 직접 검증하는 테스트 코드가 부족합니다.
*   **제안**: `pytest` 등을 도입하여 주요 연산 결과가 수학적으로 올바른지 검증하는 테스트 스위트를 구축해야 합니다.

---
위 내용에 대해 검토해 보시고, 추가로 수정이 필요한 부분이나 우선적으로 진행하고 싶은 개선 사항이 있다면 말씀해 주세요.
