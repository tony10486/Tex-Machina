# Tex-Machina Extension
## Structure

## 개선이 핑요한 사항
## 추가 예정 기능 
- Latex environment 자동 삭제 : \begin{itemize}를 지우면 쌍을 이루는 \end{itemize}가 동시에 삭제되는 기능
- cmd + shift + ; 단축키를 일정 시간동안 켜 둘 수 있도록 만들어야 함. (매번 해당 단축키를 눌러 활성화 하기보다, 해당 단측키가 고정되어 있고 esc를 눌러 이스케이프 하도록)
### Calc

### Plot 

### Macro 
- 웹뷰에서 저장된 명령어를 한눈에 보고 관리할 수 있도록 하면 좋을 것 같음 
- 키스트로크 레코딩 및 재생 속도 조절 지원.
- 매크로 미리보기 : 매크로 실행 시 예상 코드를 회색으로 미리 보여주는 기능 

### Cite
### Ref Graph
#### Frontend
### Formular search
## 고쳐야 할 버그
- label webview 레이아웃 수정이 필요함 
  토글 기능 추가 : 일부 기능은 계속 켜져 있으면 불편하므로, 토글 단축키를 눌러 모드를 활성화 한 후 작동하도록 만들 필요가 있음. 따라서 이를 위해 토글 단축키 기능을 추가하려고 함. 토글 단축키는 ctrl + ' 또는 cmd + ' 로 활성화 하며, 토글이 활성화되는 시간을 초 단위로 설정할 수 있도록 하자. vscode에서 가장 아래 부분에 작업 정보가 나타나는데, 여기에 (시계 아이콘) [토글 활성화 남은 시간]처럼 보이도록 해 줘. 가장 아래 있는 작업 바의 색깔은 초록색으로 변경되도록 하자. (작업 바가 색깔이 변경되도록 하는 것은 옵션으로 만들어서 기호에 따라 비활성화 할 수 있도록 해 줘도 좋고)
  다시 ctrl + ' 를 누르면 토글이 해제되도록 하자. 토글 단축키를 누르면 토글 활성화 상태로 진입하도록 하고, 이는 기능 중에서 토글이 활성화되어야만 작동하는 기능이 활성화될지 판단할 때 사용되는 키가 될 거야. 

  4. 숫자 첨자 자동화 (Implicit Subscripts) == 이 기능은 토글로 활성화 한 후 일정 시간 동안만 사용할 수 있도록 해서 번거롭지 않도록 하자.
   * 아이디어: 변수 이름 뒤에 숫자를 바로 붙여 쓰면 자동으로 아래첨자로 인식합니다.
       * 예: x1 -> x_1, x12 -> x_{12}, H2O -> H_{2}O
   * 효과: _ 키를 누르는 동작을 생략할 수 있어 매우 편리합니다.

  5. 액센트 단축 입력 (Accent Shorthands)
   * 아이디어: 글자 뒤에 특정 기호를 연속해서 입력하면 액센트를 씌워줍니다.
       x__ -> \bar{x}
   * 효과: 현재 Alt+. 등의 단축키보다 더 직관적이고 빠르게 입력할 수 있습니다.
  30. 문맥 인지형 점 (... vs \dots vs \cdots)
   * 디테일: .을 세 번 연속 입력(...)할 때 주변 문맥을 확인합니다.
       * 숫자나 쉼표 사이(1, 2, ...) -> \dots (하단 점)
       * 연산자 사이(+, -, \times) -> \cdots (중앙 점)
   * 편리함: 수학적으로 올바른 점의 위치를 고민할 필요 없이 ...만 치면 됩니다.

  7. 스마트 줄바꿈 (Smart Newline)
   * 아이디어: 특정 환경 내부에서 Enter를 누르면 다음 줄에 필요한 요소를 자동으로 삽입합니다.
       * align / gather 환경 안에서 Enter -> 다음 줄에 \\ &  (정렬 기호 포함) 자동 삽입
   * 효과: 환경별 반복적인 제어 문구 입력을 생략할 수 있습니다.

  8. 괄호 크기 자동 조절 (Auto \left \right) == 켜고 끌 수 있는 옵션으로 따로 빼기
   * 아이디어: 수식 모드에서 (, [, { 등을 입력할 때, 내부에 분수(\frac)나 적분(\int)과 같은 높은 요소가
     감지되면 자동으로 \left( ... \right)로 확장합니다.
   * 효과: 수식의 가독성을 위해 매번 \left, \right를 타이핑하는 수고를 덜어줍니다.

  9. 수식 전용 리가처/단축키 (Mathematical Ligatures)
   * 아이디어: 프로그래밍 폰트의 리가처처럼, 특정 기호 조합을 입력하는 즉시 LaTeX 명령어로 변환합니다.
       * << -> \ll, >> -> \gg
       * ~= -> \approx
       * != -> \neq
   * 효과: 특수 기호를 위해 백슬래시 명령어를 기억하고 타이핑하는 시간을 줄여줍니다.

  12. "최근 사용 기호" 퀵 픽 (Recent Symbols)
   * 아이디어: 수식 모드에서 특정 단축키를 누르면, 해당 문서에서 가장 많이/최근에 사용한 수학 기호(예:
     \phi, \partial) 5~10개를 커서 옆에 작은 팝업으로 띄워줍니다.
   * 효과: 긴 명령어를 반복해서 칠 때의 피로도를 줄여줍니다.

  14. 환경 이름 동기화 수정 (Linked Editing for Environments)
   * 아이디어: \begin{equation}의 이름을 align으로 수정하면 하단의 \end{equation}도 실시간으로
     \end{align}으로 함께 바뀝니다.
   * 효과: 환경을 바꿀 때 위아래를 각각 수정해야 하는 번거로움과 오타로 인한 컴파일 에러를 방지합니다.

  15. 스마트 선택 영역 확장 (Smart Selection Expansion)
   * 아이디어: 단축키(예: Ctrl+W)를 누를 때마다 선택 영역이 논리적 단위로 확장됩니다.
   예를 들어
   \begin{equation}
      \frac{x^2}{y}
   \end{equation}
   가 있고, x 뒤에 커서를 위치하면 다음과 같이 선택됨
       * x -> x^2 -> \frac{x^2}{y} -> \begin{equation} ... \end{equation}
   * 효과: 복잡한 수식의 특정 부분만 복사하거나 잘라내어 다른 곳에 붙여넣을 때 매우 빠릅니다.

  16. 표/행렬 외부 데이터 붙여넣기 (Excel/CSV to Matrix)
   * 아이디어: 엑셀이나 구글 시트에서 복사한 셀 데이터를 에디터에 붙여넣으면 자동으로 &와 \\가 붙은 matrix
     또는 tabular 코드로 변환합니다.
   * 효과: 외부 데이터를 LaTeX 표로 옮기는 고통스러운 수작업을 없애줍니다.

  18. 수식 경로 표시 (Math Context Breadcrumbs)
   * 아이디어: 현재 커서가 수식의 어느 깊이에 있는지 상태바에 표시합니다.
       * 예: align > \frac (numerator) > \sqrt
   * 효과: 중괄호가 겹겹이 쌓인 복잡한 수식에서 내가 지금 어디를 수정하고 있는지 길을 잃지 않게 해줍니다.

  19. 수식 구조 통째로 삭제 (Smart Backspace)
   * 아이디어: 커서가 비어 있는 \frac{}{}의 백슬래시(\) 위치에 있을 때 백스페이스를 누르면 구조 전체를 한
     번에 지웁니다.
   * 효과: 불필요해진 수식 구조를 지우기 위해 여러 번 백스페이스를 연타할 필요가 없습니다.

  20. 인라인/디스플레이 수식 즉시 전환 (Math Mode Toggle)
   * 아이디어: $ ... $ 내부에서 특정 단축키를 누르면 즉시 \[ ... \] 또는 \begin{equation} ...
     \end{equation}으로 변환하고 그 반대도 지원합니다.
   * 효과: 수식의 중요도에 따라 레이아웃을 바꿀 때 코드를 일일이 수정하지 않아도 됩니다.

  21. 수학적 변수 일괄 변경 (Semantic Math Refactor) == 한 줄만, 아니면 여러 동시에 바꾸기 위해선 사전에 영역 지정을 해야 할 듯
   * 아이디어: 특정 변수(예: x)를 다른 기호(예: θ)로 바꾸고 싶을 때, 일반 텍스트는 건드리지 않고 수식
     모드($...$) 내부의 x만 지능적으로 찾아 한꺼번에 변경합니다.
   * 효과: 수식 내 기호를 변경할 때 발생하는 오타와 수작업 시간을 획기적으로 줄여줍니다.

  23. 고정 수식 스니펫 검색 (Formula Library Search)
   * 아이디어: 근의 공식, 맥스웰 방정식, 테일러 급수 등 자주 쓰이는 복잡한 공식들을 이름을 검색(search >
     quadratic)하여 즉시 삽입합니다. - 더 빠른 매크로 기능으로 이식
   * 효과: 복잡한 공식의 구조를 일일이 기억하고 타이핑할 필요가 없습니다.
  
  34. 인터랙티브 행렬 리사이저 (Interactive Matrix Resizer)
   * 아이디어: 에디터의 matrix 코드 끝에 마우스를 올리면 +, - 버튼이 나타나고, 이를 클릭해 행과 열을 즉시
     추가/삭제합니다. 코드는 자동으로 &와 \\를 채워 넣습니다.
   * 효과: 행렬 구조 변경 시 수작업으로 기호를 맞추는 고통을 없애줍니다.
  
  29. 명령어와 중괄호 일괄 삭제 (Smart Backspace for Commands)
   * 디테일: \sqrt{}와 같이 비어 있는 명령어 구조에서 백스페이스를 누르면, {}만 지워지는 게 아니라 \sqrt{}
     전체가 한 번에 삭제됩니다.
   * 편리함: 실수로 입력한 명령어를 지우기 위해 백스페이스를 7~8번 연타할 필요가 없습니다.

  

  31. 선택 영역 자동 씌우기 (Wrap Selection with Scripts)
   * 디테일: 특정 텍스트(예: n+1)를 블록 지정한 상태에서 _나 ^를 누르면, 선택 영역이 자동으로 _{n+1} 또는
     ^{n+1}로 감싸집니다.
   * 편리함: 이미 써놓은 수식을 첨자로 만들 때 괄호를 앞뒤로 넣고 이동하는 수고를 덜어줍니다.
  
  34. 행렬/표 내에서 & 자동 정렬 (Smart Tab in Tables)
   * 디테일: matrix나 tabular 환경에서 Tab 키를 누르면, 단순히 공백이 들어가는 게 아니라 다음 & 위치로
     커서를 옮기거나, &가 없다면 새로 생성하고 위치를 맞춰줍니다.
   * 편리함: 표 형태의 데이터를 입력할 때 방향키를 쓸 일이 거의 없어집니다.

  36. 환경 시작 시 커서 자동 들여쓰기 (Auto-indent on Enter)
   * 디테일: \begin{env} 뒤에서 Enter를 누르면, 다음 줄에 자동으로 들여쓰기(Indentation)를 적용하고 커서를
     배치합니다.
   * 편리함: 코드의 계층 구조가 자동으로 잡혀 수식이나 환경의 범위를 파악하기 좋아집니다.
